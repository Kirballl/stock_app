version: '3'
services:
  database:
    image: postgres:16.3
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: thunderbolt
    ports:
      - "5432:5432"

